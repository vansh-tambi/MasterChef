# Master Chef Client Configuration
# Set this if hosting frontend on a different domain than your backend (e.g. Vercel frontend + Render backend)
# When empty or omitted, requests use same-origin relative URLs (/api/...)
VITE_API_URL=https://masterchef-hbdd.onrender.com
